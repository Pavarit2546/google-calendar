# ----------------------------------------------------
# Google Calendar Integration Configuration
# This file serves as a template. Copy it to .env
# and fill in your actual credentials.
# DO NOT commit the .env file to your repository!
# ----------------------------------------------------

# 1. Google OAuth 2.0 Credentials
# Get these from the Google API Console.
CLIENT_ID=""
CLIENT_SECRET=""

# 2. Google OAuth 2.0 Endpoints
# Standard Google endpoints. Should generally not need changing.
TOKEN_ENDPOINT="https://oauth2.googleapis.com/token"
AUTH_URL="https://accounts.google.com/o/oauth2/v2/auth"

# 3. Redirect URI
# The exact URI registered in the Google API Console.
# If using ngrok, this needs to be updated when ngrok restarts.
REDIRECT_URI="https://your-domain/auth/google/callback"

# 4. Google Calendar API Details
# The base URL for the Calendar Events API and the scope required.
CALENDAR_API_BASE="https://www.googleapis.com/calendar/v3/calendars/primary/events"
CALENDAR_SCOPE="https://www.googleapis.com/auth/calendar"

# 5. Global Tokens (For testing or continuous service account)
# A refresh token obtained after the first authorization.
# Keep this highly secure in the actual .env file.
GLOBAL_REFRESH_TOKEN=""